<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.ubiobio.serviciodesaludbio_bio">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SS Bío-Bío"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:label="@string/app_name" -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMayorActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainJovenActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- android:label="@string/title_activity_joven" -->
        </activity>
        <activity android:name=".HVisitaActivity" />
        <activity android:name=".CHoraActivity" />
        <activity android:name=".DSangreActivity" />
        <activity android:name=".CEstadoActivity" />
        <activity android:name=".CServiciosActivity" />
        <activity android:name=".FarmaciaActivity" />
        <activity android:name=".SaludRespondeActivity" />
        <activity android:name=".SinSaldoActivity" />
    </application>

</manifest>